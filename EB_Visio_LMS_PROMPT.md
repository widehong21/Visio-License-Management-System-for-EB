# 🤖 EB Visio LMS — GPT Prompt Engineering 가이드

> EB Visio LMS (`EB_Visio_LMS_v1.html`) 개발·유지보수·기능 확장 시  
> GPT(ChatGPT / Claude)에 효과적으로 질의하기 위한 프롬프트 템플릿 모음집

---

## 📌 필수 공통 컨텍스트 (모든 질문 앞에 붙이기)

> **아래 블록을 모든 프롬프트 맨 앞에 복사·붙여넣기 하세요.**

```
[시스템 컨텍스트]
나는 EB Visio LMS (EB_Visio_LMS_v1.html)를 유지보수하고 있다.
이 시스템의 핵심 스펙은 다음과 같다:

- 단일 HTML 파일 (약 2,742줄), Vanilla JS + CSS, 외부 라이브러리 없음
- 데이터 저장: JSONBin.io REST API (멀티유저 공유) + localStorage (캐시)
- API Key: $2a$10$AnfRJklYwNs79NBLSvzdnea3IUCQEdvK.98YiahGPazxSMWmoQg06
- Bin ID: 6a39b74df5f4af5e291fcff3
- 동기화: 15초 폴링 + 탭 복귀 + F5 + 저장 즉시(300ms throttle)
- 전역 상태: DB 객체 (users / pools / licenses / requests / audit_logs / nextId)
- 현재 로그인 사용자: CU 변수
- 라이선스 유형: Process (의장 diagram) / Detail (전장 diagram)
- 관리자: is_admin:true 사용자 / 시스템 관리자: employee_id='0000', name='Admin'
- 페이지: dashboard / apply / revoke / status / approval / admin / search
- 관리자 탭: users / perms / lic / pool / history / sys
- 모든 변경 시 addLog() 호출 후 saveDB() 필수
- UI 갱신: 변경 후 renderXxx() 함수 호출 필요
- 경고: HTML 단일 파일이므로 외부 모듈/import 사용 불가
```

---

## 🗂️ 프롬프트 카테고리

1. [기능 추가](#1-기능-추가)
2. [버그 수정](#2-버그-수정)
3. [UI 개선](#3-ui-개선)
4. [데이터 구조 변경](#4-데이터-구조-변경)
5. [성능 최적화](#5-성능-최적화)
6. [코드 분석 · 설명 요청](#6-코드-분석--설명-요청)
7. [동기화 · JSONBin 관련](#7-동기화--jsonbin-관련)
8. [Excel 연동](#8-excel-연동)
9. [보안 강화](#9-보안-강화)
10. [전체 리팩토링](#10-전체-리팩토링)

---

## 1. 기능 추가

### 1-A. 새 메뉴 페이지 추가

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
EB Visio LMS에 "라이선스 만료 관리" 페이지를 추가해줘.

[요구 사항]
- 사이드바에 새 메뉴 항목 추가: "📅 만료 관리"
- 페이지 ID: page-expiry
- 관리자만 접근 가능
- Pool 데이터에 expiry_date 필드 추가 (YYYY-MM-DD)
- 만료 30일 전 항목은 주황색, 만료된 항목은 빨간색으로 표시
- Dashboard KPI에 "만료 임박" 건수 추가

[출력 형식]
수정이 필요한 코드 블록만 발췌하여 줘.
각 블록 앞에 해당 위치(줄 번호 또는 함수명)를 명시해.
기존 코드를 최대한 건드리지 말고 추가 방식으로 구현해.
```

---

### 1-B. 새 필드를 기존 테이블에 추가

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
Pool 객체에 purchase_date(구매일)와 vendor(공급사) 필드를 추가해줘.

[요구 사항]
- INIT.pools 각 항목에 기본값 추가 (purchase_date:'', vendor:'')
- Pool 추가 폼(admin-tab-pool)에 입력 필드 추가
- Pool 목록 테이블에 컬럼 추가
- Excel 업로드 시 해당 컬럼 자동 인식 (한글: '구매일', '공급사')
- Excel 다운로드 시 해당 컬럼 포함

[제약]
기존 DB에 해당 필드가 없을 경우 undefined 대신 빈 문자열로 fallback 처리할 것
```

---

### 1-C. 통계 차트 추가

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
Dashboard에 "월별 신청 건수" 라인 차트를 추가해줘.

[요구 사항]
- Chart.js (이미 로드됨, 전역 Chart 객체 사용)
- 최근 6개월 데이터를 DB.requests에서 집계
- 신청(apply) / 해지(revoke) 두 계열로 구분
- 기존 도넛/막대 차트 아래에 배치
- 차트 인스턴스는 기존 donutChart, barChart처럼 전역 변수로 관리
- renderDashboard() 함수 내 호출

[참고 코드]
기존 barChart 초기화 방식과 동일한 패턴으로 구현해줘.
```

---

### 1-D. 알림/배지 기능 추가

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
관리자 로그인 시 대기 중인 요청 건수를 사이드바 메뉴 뱃지로 표시해줘.

[요구 사항]
- "✅ 승인 관리" 메뉴 우측에 숫자 뱃지 (빨간 원 형태)
- 대기 건수 = DB.requests.filter(r => r.status === 'pending').length
- 0건이면 뱃지 숨김
- 페이지 전환 / 승인·반려 처리 시 자동 갱신
- 비관리자에게는 표시 안 함
```

---

## 2. 버그 수정

### 2-A. 특정 함수 버그 보고

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[버그 상황]
함수: submitApply()
증상: 라이선스 신청 후 "가용 라이선스가 없습니다" 오류가 뜨는데
      실제로는 Pool에 미배정 항목이 있음
재현: Process 유형으로 신청할 때만 발생, Detail은 정상

[의심 원인]
getNextAvailPool() 함수에서 is_license_deleted 체크 로직 문제로 의심

[요청]
submitApply()와 getNextAvailPool()의 전체 코드를 분석하고
버그 원인과 수정된 코드를 제시해줘.
```

---

### 2-B. 동기화 충돌 버그

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[버그 상황]
두 명이 동시에 데이터를 수정하면 마지막 저장자의 데이터가 이전 데이터를 덮어씀.
Last-Write-Wins 방식의 JSONBin.io 특성상 발생.

[요청]
충돌을 최소화하는 방법을 제안해줘.
- 옵션 A: 저장 직전 원격 최신 데이터 확인 후 병합 로직 추가
- 옵션 B: nextId 기반 타임스탬프 서명으로 충돌 감지 후 사용자 알림
어떤 방식이 현 구조에 더 적합한지 설명하고, 선택한 방식의 구현 코드를 줘.
```

---

### 2-C. UI 렌더 이상

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[버그 상황]
renderAdminUsers() 호출 후 테이블이 비어 있음.
DB.users에는 데이터가 있는 것을 console.log로 확인함.

[제공 코드]
// renderAdminUsers 함수 전체 붙여넣기
function renderAdminUsers() {
  // ...
}

[요청]
위 코드에서 DOM 업데이트가 실패하는 원인을 찾고 수정안을 줘.
```

---

## 3. UI 개선

### 3-A. 반응형 모바일 지원

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
현재 desktop 전용인 레이아웃을 모바일(360px~768px)에서도 동작하도록 개선해줘.

[요구 사항]
- 사이드바: 모바일에서는 숨기고 햄버거 버튼으로 토글
- 테이블: 모바일에서 가로 스크롤 가능하도록
- 폼: 2열 그리드 → 모바일에서 1열
- KPI 그리드: 5열 → 모바일 2열
- 기존 CSS 변수 체계 유지 (`:root { --blue, --gray1 ... }`)

[제약]
외부 CSS 프레임워크 사용 불가. 순수 미디어 쿼리만 사용.
기존 class명 변경 최소화.
```

---

### 3-B. 다크 모드 추가

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
시스템 다크 모드를 지원하도록 CSS를 추가해줘.

[요구 사항]
- `prefers-color-scheme: dark` 미디어 쿼리 사용
- 기존 CSS 변수를 dark 모드용으로 오버라이드
- 토글 버튼으로 수동 전환도 가능 (localStorage에 preference 저장)
- 로그인 화면·사이드바·카드·테이블 모두 적용

[스타일 가이드]
기존 --gray6(#FAFAFA) → dark: #1a1a1a
기존 --blue(#0071E3) → dark: #34AADC (더 밝게)
```

---

### 3-C. 테이블 정렬 기능 추가

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
현황 조회(renderStatus) 테이블에 컬럼 클릭 정렬 기능을 추가해줘.

[요구 사항]
- 클릭 시 오름차순/내림차순 토글
- 정렬 중인 컬럼에 ▲/▼ 아이콘 표시
- 정렬 대상 컬럼: 이름, 사번, 그룹, 등록일, 상태
- 정렬 상태는 현재 세션만 유지 (새로고침 초기화 가능)
- renderStatus() 함수를 최소한으로 수정

[출력]
수정된 renderStatus() 함수 전체와 관련 CSS 추가분만 출력해줘.
```

---

## 4. 데이터 구조 변경

### 4-A. 기존 DB 마이그레이션

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[상황]
기존 Pool 객체에 is_license_deleted 필드가 없는 구 버전 데이터가
일부 사용자의 localStorage에 남아있음.

[요청]
앱 시작 시 구 버전 데이터를 자동 마이그레이션하는 함수를 작성해줘.

[마이그레이션 규칙]
- pools[].is_license_deleted 없으면 false로 초기화
- pools[].assigned_knox 없으면 '' 초기화
- licenses[].assigned_by 없으면 'Unknown' 초기화
- 마이그레이션 완료 시 audit_log에 'MIGRATE' 액션 기록
- initDBFromIDB() 함수 내에서 호출

[주의]
JSONBin의 원격 데이터도 마이그레이션 후 다시 저장해야 함.
```

---

### 4-B. 라이선스 유형 동적 관리

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
현재 하드코딩된 라이선스 유형('Process', 'Detail')을 관리자가 동적으로 추가/삭제할 수 있도록 개선해줘.

[요구 사항]
- DB에 license_types 배열 추가 (초기값: ['Process', 'Detail'])
- 관리자 설정 > 시스템 정보 탭에 유형 관리 UI 추가
- 모든 select 드롭다운을 DB.license_types 기반으로 동적 렌더
- 유형 삭제 시 해당 유형의 Pool이 있으면 삭제 불가 경고
- INIT 및 ensureSystemAdmin()에서 기본값 보장

[영향 받는 함수]
fillApplyPool, onGaLT, refreshNsSerial, refreshAlmSerial,
addPool, renderPool, renderStatus, handleAdminLicUpload 등
각 함수의 하드코딩된 'Process'/'Detail' 참조를 DB.license_types 기반으로 변경해줘.
```

---

## 5. 성능 최적화

### 5-A. 대용량 데이터 대응

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[상황]
audit_logs가 1,000건 이상 쌓이면서 렌더링이 느려짐.
현황 조회 테이블도 500건 이상일 때 스크롤 렌더가 버벅임.

[요청]
다음 두 가지 최적화를 적용해줘:

1. renderHistory() — 페이지네이션 추가
   - 한 페이지 50건, 이전/다음 버튼
   - 현재 선택 limit select (100/200/500건)는 유지

2. renderStatus() — 가상 스크롤 또는 지연 로딩
   - 처음 50건만 렌더, 스크롤 하단 도달 시 50건 추가 로드
   - 필터 변경 시 초기화

[제약]
외부 라이브러리 추가 없이 순수 JS만 사용.
```

---

### 5-B. JSONBin 요청 최소화

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[상황]
무료 플랜 월 10,000 요청 한도에 근접하고 있음.
현재: 15초 폴링 = 하루 약 5,760 req (24h × 4req/min)

[요청]
JSONBin 요청 수를 줄이는 최적화 방안을 제안하고 구현해줘.

[아이디어]
- 폴링 주기를 비활성 탭일 때는 60초로 늘리기
- ETag/If-None-Match 헤더로 변경 없을 때 바이트 절약
- 서명(_makeSig) 비교 전에 304 응답 활용

[출력]
syncFromShared() 및 startAutoSync() 개선 버전 전체 코드.
```

---

## 6. 코드 분석 · 설명 요청

### 6-A. 특정 함수 동작 설명

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
아래 함수의 동작을 단계별로 설명해줘.
신입 개발자가 이해할 수 있도록 한국어로, 각 줄에 주석도 달아줘.

[코드]
// doApprove 함수 전체 붙여넣기
function doApprove(reqId) {
  // ...
}
```

---

### 6-B. 전체 코드 리뷰

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
EB_Visio_LMS_v1.html 전체 코드를 리뷰해줘.

[검토 항목]
1. XSS 취약점 (innerHTML 사용 시 esc() 함수 미적용 부분)
2. 메모리 누수 가능성 (이벤트 리스너, Chart.js 인스턴스)
3. 에러 핸들링 누락 구간
4. 코드 중복 (DRY 원칙 위반)
5. 비동기 처리 오류 가능성 (async/await 미사용 구간)

[출력 형식]
항목별로 나누어,
- 위험도 (High/Medium/Low)
- 해당 함수/위치
- 문제 설명
- 수정 방향
순으로 정리해줘.
```

---

### 6-C. 데이터 흐름 분석

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
라이선스 신청부터 배정 완료까지 데이터 흐름을 추적해줘.

구체적으로:
- submitApply() 호출 시 DB 상태 변화
- doApprove() 호출 시 DB 상태 변화 (Request / Pool / License 각각)
- 각 단계에서 saveDB()와 addLog() 호출 시점
- 동기화까지 완료되는 전체 타임라인 (ms 단위 추정 포함)

Mermaid 시퀀스 다이어그램으로도 출력해줘.
```

---

## 7. 동기화 · JSONBin 관련

### 7-A. 오프라인 지원 추가

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
인터넷 연결이 끊겼을 때도 시스템이 동작하도록 오프라인 모드를 추가해줘.

[요구 사항]
- navigator.onLine 감지
- 오프라인 시: localStorage 데이터로만 동작, 상단 배너로 "오프라인 모드" 표시
- 온라인 복귀 시: 로컬 변경사항 자동 업로드 (충돌 처리 포함)
- 변경사항 큐: 오프라인 중 발생한 저장 요청을 큐에 쌓고 온라인 복귀 시 순차 처리

[연관 함수]
saveDB(), syncFromShared(), _syncUI() 수정 필요
```

---

### 7-B. 다른 백엔드로 마이그레이션

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
JSONBin.io 대신 Firebase Realtime Database를 사용하도록 변경해줘.

[변경 범위]
- sharedGet() → Firebase get()으로 교체
- sharedSet() → Firebase set()으로 교체
- 폴링 대신 Firebase onValue() 리얼타임 리스너 사용 (더 효율적)
- startAutoSync() 제거 또는 대체

[주의]
Firebase SDK는 CDN으로 추가해줘 (단일 HTML 파일 유지).
Firebase 설정 객체는 상수로 분리해서 쉽게 변경 가능하도록.
```

---

### 7-C. 동기화 상태 디버깅

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[상황]
특정 사용자 PC에서 동기화가 되지 않는다는 보고를 받음.
_syncUI('err')가 표시되고 있음.

[요청]
1. 동기화 실패의 가능한 원인 목록을 우선순위 순으로 정리해줘.
2. 브라우저 콘솔에서 수동으로 동기화 상태를 진단하는 스크립트를 작성해줘.
3. syncFromShared() 함수에 상세 에러 로깅을 추가해줘 (어느 단계에서 실패했는지 명확히).
```

---

## 8. Excel 연동

### 8-A. Excel 업로드 컬럼 확장

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
Pool Excel 업로드 시 인식하는 컬럼을 확장해줘.

[현재 인식 컬럼]
시리얼번호, 라이선스유형, 등록일, remark

[추가할 컬럼]
- 구매일 (purchase_date): 한글 '구매일', 영문 'purchase_date', 'PurchaseDate'
- 공급사 (vendor): 한글 '공급사', '벤더', 영문 'vendor', 'Vendor'
- 만료일 (expiry_date): 한글 '만료일', 영문 'expiry_date', 'ExpiryDate'

[연관 함수]
normalizeCol(), handlePoolUpload(), handleAdminLicUpload()
각 함수에서 새 컬럼 처리 로직 추가해줘.
```

---

### 8-B. 맞춤형 Excel 템플릿 다운로드

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
Pool 업로드용 Excel 템플릿을 다운로드하는 버튼을 추가해줘.

[템플릿 내용]
헤더 행: 시리얼번호 | 라이선스유형 | 등록일 | Remark
예시 행 2줄 포함 (VISIO-2024-XXXX | Process | 2026-06-01 | 예시)
헤더 행 배경색: 파란색(0071E3), 흰색 글씨
SheetJS로 생성

[위치]
Pool 관리 > Excel 업로드 탭 상단에 "📄 템플릿 다운로드" 버튼 추가
```

---

## 9. 보안 강화

### 9-A. XSS 방지 강화

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[현재 상황]
esc() 함수가 있지만 일부 innerHTML 사용 시 누락된 부분이 있을 수 있음.

[요청]
1. 전체 코드에서 innerHTML을 사용하는 모든 위치를 찾아줘.
2. 각 위치에서 사용자 입력 데이터가 esc() 처리 없이 삽입되는 곳을 표시해줘.
3. 위험한 곳은 수정된 코드를 제시해줘.

[참고 esc 함수]
const esc = s => String(s??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
```

---

### 9-B. API Key 숨기기

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[상황]
JSONBIN_API_KEY가 HTML 파일에 평문으로 포함되어 있어
GitHub Pages 배포 시 공개 저장소라면 노출될 위험이 있음.

[요청]
GitHub Pages 배포 환경에서 API Key를 숨기는 현실적인 방법을 제안해줘.

[고려 사항]
- 단일 HTML 파일 구조 유지 (빌드 도구 없음)
- GitHub Actions 사용 가능
- 무료 방안 우선
- 완벽한 보안보다는 실용적인 난독화 수준도 검토

[옵션 분석]
1. GitHub Actions로 배포 시 Secrets 주입
2. 환경변수 방식 (gh-pages 브랜치 별도 관리)
3. 암호화 후 런타임 복호화
각 방안의 장단점과 구현 방법을 알려줘.
```

---

## 10. 전체 리팩토링

### 10-A. 모듈화 제안

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
현재 단일 파일 2,742줄 구조를 논리적으로 모듈화한다면 어떻게 분리하는 게 좋을지 설계안을 제시해줘.

[출력 형식]
파일명 / 담당 기능 / 포함 함수 목록 / 의존성
형태의 표로 정리해줘.

[제약]
실제로 분리하지 말고 설계안만 제시. 
추후 Vite/Rollup 등으로 번들링할 때 참고할 구조로.
```

---

### 10-B. TypeScript 타입 정의

```
[시스템 컨텍스트] (위 블록 붙여넣기)

[요청]
현재 JS 코드의 주요 데이터 구조를 TypeScript 인터페이스/타입으로 정의해줘.

[대상]
- User, Pool, License, Request, AuditLog 인터페이스
- DB 전체 타입
- 주요 함수 시그니처 (renderDashboard, addPool, doApprove 등 10개)
- 라이선스 유형, 요청 상태, 감사 액션 Enum

[용도]
실제 TS 변환이 아니라 개발 문서 및 AI 질의 시 컨텍스트 제공용.
```

---

## 💡 프롬프트 작성 팁

### ✅ 효과적인 프롬프트 요소

| 요소 | 예시 |
|------|------|
| **컨텍스트 제공** | "현재 `saveDB()`는 300ms throttle을 사용한다" |
| **범위 제한** | "수정이 필요한 함수만 출력해줘" |
| **형식 지정** | "Mermaid 다이어그램으로 출력해줘" |
| **제약 명시** | "외부 라이브러리 추가 없이", "기존 class명 유지" |
| **예시 포함** | "기존 barChart 패턴과 동일하게" |
| **우선순위** | "성능보다 코드 가독성 우선" |

### ❌ 피해야 할 패턴

```
❌ "전체 코드 다시 짜줘"
   → GPT 컨텍스트 한도 초과, 기존 기능 누락 위험

❌ "이거 고쳐줘" (코드 없이)
   → 충분한 컨텍스트 없으면 엉뚱한 답변

❌ "더 좋게 만들어줘"
   → 목표가 불명확해 원하지 않는 방향으로 수정 가능

✅ 대신: "renderStatus() 함수에서 [특정 문제]를 [특정 방식]으로 수정해줘"
```

### 📐 코드 수정 요청 황금 템플릿

```
[시스템 컨텍스트] (위 필수 블록)

[현재 코드]
// 수정이 필요한 함수/부분만 여기에 붙여넣기

[문제 / 요청]
// 무엇이 문제인지 또는 무엇을 추가해야 하는지

[요구 사항]
// 구체적인 조건 목록 (번호 매기기)

[제약 사항]
// 변경하면 안 되는 것들

[출력 형식]
// 전체 함수 / 수정된 부분만 / 특정 형식 지정
```

---

## 📁 관련 파일

| 파일 | 설명 |
|------|------|
| `EB_Visio_LMS_v1.html` | 시스템 본체 (단일 파일) |
| `EB_Visio_LMS_README.md` | 시스템 기술 문서 |
| `EB_Visio_LMS_PROMPT.md` | 이 파일 — GPT 프롬프트 가이드 |

---

*EB · Visio LMS · Prompt Engineering Guide · v1.1 · 2026.06*
