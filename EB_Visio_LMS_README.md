# 🔷 EB Visio LMS (License Management System) — ver 1.0

> **단일 HTML 파일**로 동작하는 웹 기반 Visio 소프트웨어 라이선스 관리 시스템입니다.  
> 별도 서버 없이 브라우저에서 바로 실행되며, 모든 데이터는 **IndexedDB**에 영구 저장됩니다.

---

## 📋 목차

1. [시스템 개요](#시스템-개요)
2. [실행 방법](#실행-방법)
3. [로그인 안내](#로그인-안내)
4. [화면 구성](#화면-구성)
5. [데이터베이스 구조](#데이터베이스-구조)
6. [주요 기능 상세](#주요-기능-상세)
7. [관리자 기능](#관리자-기능)
8. [데이터 입출력](#데이터-입출력)
9. [운영 규칙](#운영-규칙)
10. [버전 이력](#버전-이력)

---

## 시스템 개요

| 항목 | 내용 |
|---|---|
| 시스템명 | EB Visio LMS (License Management System) |
| 버전 | ver 1.0 |
| 파일 형식 | 단일 HTML 파일 (`EB_Visio_LMS_v3.html`) |
| 데이터 저장소 | IndexedDB (`eb_visio_lms_db`) |
| 백업 저장소 | localStorage (`eb_lms`) |
| 지원 브라우저 | Chrome, Edge (최신 버전 권장) |
| 외부 의존성 | 없음 (완전 오프라인 동작) |

### 라이선스 유형

| 유형 | 용도 |
|---|---|
| **Process** | 프로세스 흐름도 작성 |
| **Detail** | 전장 Diagram 상세 작성 |

---

## 실행 방법

1. `EB_Visio_LMS_v3.html` 파일을 Chrome 또는 Edge 브라우저로 열기
2. 로그인 화면에서 이름과 사번 입력 후 로그인
3. **F5** 키: 현재 화면 새로고침 (로그인 상태 유지)

---

## 로그인 안내

| 계정 유형 | 이름 | 사번 | 권한 |
|---|---|---|---|
| 시스템 관리자 | Admin | 0000 | 전체 기능 |
| 일반 사용자 | 등록된 이름 | 등록된 사번 | 신청/조회 |
| 테스트 | test | test | 일반 사용자 |

> ⚠️ 로그인은 **이름 + 사번** 조합이 DB에 등록된 사용자만 가능합니다.  
> 미등록자는 로그인 화면의 **📝 라이선스 등록 신청** 버튼을 통해 Guest 신청이 가능합니다.

---

## 화면 구성

```
┌─────────────────────────────────────────────────────────┐
│  사이드바                    메인 컨텐츠 영역              │
│  ┌────────────────┐         ┌────────────────────────┐  │
│  │ 🔷 EB Visio    │         │  현재 페이지 내용        │  │
│  │  LMS (ver 1.0) │         │                        │  │
│  │  License Mgmt  │         │                        │  │
│  ├────────────────┤         │                        │  │
│  │ 📊 Dashboard   │         │                        │  │
│  │ 📋 라이선스 신청│         │                        │  │
│  │ 🔄 라이선스 해지│         │                        │  │
│  │ 📄 나의 현황   │         │                        │  │
│  │ ✅ 승인 관리   │         │                        │  │
│  │ ⚙️ 관리자 설정  │         │                        │  │
│  ├────────────────┤         │                        │  │
│  │ [사용자 정보]  │         │                        │  │
│  │ [로그아웃]     │         │                        │  │
│  └────────────────┘         └────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 페이지별 접근 권한

| 페이지 | 일반 사용자 | 관리자 |
|---|:---:|:---:|
| 📊 Dashboard | ✅ | ✅ |
| 📋 라이선스 신청 | ✅ | ✅ |
| 🔄 라이선스 해지 | ✅ | ✅ |
| 📄 나의 현황 | ✅ | ✅ |
| ✅ 승인 관리 | ❌ | ✅ |
| ⚙️ 관리자 설정 | ❌ | ✅ |

---

## 데이터베이스 구조

### users 테이블

| 필드 | 타입 | 설명 |
|---|---|---|
| id | number | 고유 ID |
| employee_id | string | 사번 (로그인 키) |
| name | string | 이름 (로그인 키) |
| group | string | 소속 그룹/팀 |
| knox_account | string | Knox 계정 (이메일) |
| is_admin | boolean | 관리자 여부 |
| is_active | boolean | 활성 여부 |
| created_at | string | 등록일 |

### pools 테이블 (라이선스 시리얼번호 마스터)

| 필드 | 타입 | 설명 |
|---|---|---|
| id | number | 고유 ID |
| serial_number | string | Visio 시리얼번호 |
| license_type | string | Process / Detail |
| status | string | active |
| is_assigned | boolean | 배정 여부 |
| is_license_deleted | boolean | 삭제 플래그 (행 유지) |
| assigned_user_name | string | 배정된 사용자명 |
| assigned_employee_id | string | 배정된 사번 |
| assigned_group | string | 배정된 그룹 |
| assigned_knox | string | 배정된 Knox 계정 |
| registered_date | string | 등록일 |
| registered_by | string | 등록자 |
| remark | string | 비고 |

### licenses 테이블 (배정 이력)

| 필드 | 타입 | 설명 |
|---|---|---|
| id | number | 고유 ID |
| user_id | number | 사용자 ID |
| pool_id | number | Pool ID |
| status | string | active / revoked |
| assigned_date | string | 배정일 |
| revoked_date | string | 회수일 |
| assigned_by | string | 배정 처리자 |
| remark | string | 비고 |

### requests 테이블 (신청/해지 요청)

| 필드 | 타입 | 설명 |
|---|---|---|
| id | number | 고유 ID |
| requester_id | number | 신청자 user ID |
| request_type | string | apply / revoke |
| pool_id | number | 대상 Pool ID |
| status | string | pending / approved / rejected / cancelled |
| reason | string | 신청 사유 |
| admin_comment | string | 관리자 코멘트 |
| approved_by | string | 승인자 |
| approved_at | string | 승인일시 |
| created_at | string | 신청일시 |

### audit_logs 테이블 (변경 이력)

| 필드 | 타입 | 설명 |
|---|---|---|
| id | number | 고유 ID |
| action | string | CREATE / UPDATE / DELETE / APPROVE / REJECT / REVOKE |
| table_name | string | 대상 테이블 |
| record_id | number | 대상 레코드 ID |
| user_id | number | 처리자 ID |
| username | string | 처리자명 |
| description | string | 상세 내용 |
| created_at | string | 처리 일시 |

---

## 주요 기능 상세

### 📊 Dashboard

- 라이선스 유형별 전체 / 배정됨 / 가용 / 이용률 통계 카드
- 라이선스 유형별 현황 테이블 (Process / Detail)
- Pool 배정 현황 도넛 차트
- 최근 7일 내 배정된 신규 라이선스 목록
- 전체 사용자 라이선스 현황 테이블

### 📋 라이선스 신청

- 현재 보유 라이선스 확인
- 라이선스 유형(Process / Detail) 선택 후 신청
- 신청 사유 필수 입력
- 가용 Pool 수량 실시간 표시
- 관리자 승인 후 자동 배정

### 🔄 라이선스 해지

- 보유 중인 라이선스 해지 신청
- 해지 사유 입력
- 관리자 승인 후 Pool 반납

### 📄 나의 현황

- 현재 배정된 라이선스 정보 조회
- 전체 라이선스 이력 (배정/회수) 조회
- 라이선스 현황 Excel 다운로드

---

## 관리자 기능

### ⚙️ 관리자 설정 탭 구성

#### 👥 사용자 관리
- 등록된 사용자 목록 조회 (ID / 이름 / 사번 / 그룹 / Knox 계정 / 관리자 여부 / 활성 / 라이선스 배정 현황)
- **라이선스 등록** 버튼: 미배정 사용자에게 직접 라이선스 배정 (유형 선택 → 가용 시리얼 선택 → 등록)
- 사용자 추가: 이름 / 사번 / 그룹 / Knox 계정 / Visio 시리얼번호(선택) 입력
- 강제 삭제: 활성 라이선스 자동 회수 후 비활성화

#### 🔑 관리자 권한
- 관리자 목록 조회
- 일반 사용자 → 관리자 권한 부여
- 관리자 권한 해제 (시스템 관리자 Admin/0000은 해제 불가)

#### 💿 라이선스 등록/삭제

| 기능 | 설명 |
|---|---|
| 라이선스 등록 | 시리얼번호 / 유형 / 등록일 입력하여 Pool에 추가 |
| License 삭제 | 배정 해제 → 배정상태 **가용** 으로 변경, 사용자/사번 정보 삭제 (행 유지) |
| 완전 삭제 | 해당 행 자체 완전 제거 |

> **License 삭제 vs 완전 삭제**  
> - License 삭제: 시리얼번호·유형은 목록에 유지, 배정 정보만 초기화 → 재배정 가능  
> - 완전 삭제: 해당 시리얼번호 행 자체 삭제

#### 🗂️ Pool 관리

**Pool 목록**
- 전체 Pool 조회 (필터: 유형 / 배정 상태)
- Excel 다운로드

**Pool 단건 추가**
- 라이선스 유형 선택 → 가용 시리얼번호 드롭다운 (미배정 항목만 표시)
- 사용자 배정 (이름 / 사번 / 그룹 / Knox 계정 — 모두 필수)
- 등록일 / Remark 입력

**Excel 업로드**
- `.xlsx` 파일로 일괄 Pool 등록
- 필수 컬럼: `시리얼번호`, `라이선스유형`, `등록일자`
- 선택 컬럼: `사용자`, `사번`, `그룹`, `Knox계정`, `Remark`

#### 📜 변경 이력
- 전체 CRUD 이력 조회
- 이력 초기화 기능

#### 🔧 시스템 정보
- 저장소 / 저장 크기 / DB 통계 (사용자 / Pool / 라이선스 / 요청 / 감사로그 건수)
- 데이터 초기화 버튼

### ✅ 승인 관리

- **대기 중인 요청**: 신청/해지 요청 승인 또는 반려 (코멘트 입력 가능)
- **전체 요청 이력**: 유형 / 상태별 필터 조회

---

## 데이터 입출력

### Excel 업로드 (Pool 일괄 등록)

```
필수 컬럼:
  - 시리얼번호
  - 라이선스유형  (Process 또는 Detail)
  - 등록일자      (YYYY-MM-DD)

선택 컬럼:
  - 사용자
  - 사번
  - 그룹
  - Knox계정
  - Remark
```

### Excel 다운로드

| 다운로드 위치 | 내용 |
|---|---|
| 나의 현황 | 개인 라이선스 이력 |
| Pool 관리 목록 | 전체 Pool 현황 |

---

## 운영 규칙

1. **시리얼번호 중복** — 동일 시리얼번호는 Pool에 하나만 존재
2. **Pool 추가 시 시리얼번호** — 미배정(가용) 상태 항목만 선택 가능
3. **사용자 1인 1라이선스** — 활성 라이선스 보유 중 추가 신청 불가
4. **License 삭제** — 배정 해제 후 가용 상태 전환 (행 유지, 재배정 가능)
5. **시스템 관리자(Admin/0000)** — 삭제 불가 / 권한 해제 불가 / 라이선스 배정 제외
6. **F5 새로고침** — 현재 페이지 유지 상태에서 화면만 재렌더링
7. **로그인 공지 문구** — 관리자만 편집 가능 (로그인 화면 하단)

---

## 버전 이력

| 버전 | 주요 변경 내용 |
|---|---|
| v1.0 | 최초 릴리즈 — 사용자 관리, Pool 관리, 라이선스 신청/승인, Dashboard |
| v2.0 | Pool 단건 추가 시리얼번호 드롭다운 / Knox 계정 필수 입력 |
| v3.0 | 사용자/사번/그룹 필수 입력 / 사이드바 로고 2줄 / 로그인 공지 문구 (관리자 편집) / F5 새로고침 / License 삭제 → 가용 전환 / 사용자 관리 탭 라이선스 등록 버튼 |

---

> 문의: 시스템 관리자 (Admin / 사번 0000)
